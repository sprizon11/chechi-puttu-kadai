org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Kotlin incremental caches break when Flutter plugins live under C: (Pub cache) while the project is on another
# drive (e.g. D:). SYM: "different roots" IllegalArgumentException in google_sign_in_android compileDebugKotlin.
kotlin.incremental=false
